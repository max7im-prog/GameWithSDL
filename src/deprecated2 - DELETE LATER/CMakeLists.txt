file(GLOB_RECURSE SOURCES CONFIGURE_DEPENDS
    "*.cpp"
    "*.hpp"
)

set(CREATURE_SRC ${SOURCES} PARENT_SCOPE)

set(CREATURE_INCLUDE_DIRS
    ${CMAKE_CURRENT_SOURCE_DIR}
    # ${CMAKE_CURRENT_SOURCE_DIR}/bodyParts
    PARENT_SCOPE
)
